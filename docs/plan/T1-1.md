# T1-1 protocol 型

- モデル: sonnet
- 依存: T0-2
- 参照: spec §2、README §4.1
- 変更ファイル: `crates/protocol/Cargo.toml`、`crates/protocol/src/lib.rs`、`crates/protocol/src/tests.rs`

## テスト（先に書く）

- `hello_roundtrip`: `AgentMsg::Hello` を JSON にして戻すと同値。JSON に `"type":"hello"` を含む
- `reply_roundtrip`
- `permission_request_roundtrip`
- `inbound_roundtrip`
- `verdict_serializes_lowercase`: `"behavior":"allow"`
- `unknown_type_rejected`: `{"type":"nope"}` はエラー
- `unknown_field_rejected`: Reply に余分なキーがあるとエラー
- `version_constant_is_1`

## 実装

- README §4.1 のコードそのまま
- 依存は `serde`（derive）と `serde_json` のみ

## 完了条件

- 共通の完了条件（fmt / clippy / test）
- コミット `feat(protocol): add relay-agent message types`
