# T0-1 リポジトリ骨格

状態: 完了

- git init（main ブランチ）
- cargo workspace（`crates/*`、resolver 3）
- `protocol`（lib）、`sccr-agent`（bin）、`sccr-relay`（bin）の 3 クレート
- `.gitignore`、`rustfmt.toml`、`.github/workflows/ci.yml`（fmt / clippy -D warnings / test）
- `docs/plan/` に計画とタスクカードを配置
