# T4-2 Slack events の分類

- モデル: sonnet
- 依存: T0-2（T4-1 と同時に進めるなら `slack/mod.rs` の衝突に注意）
- 参照: spec §5.2、§5.3
- 変更ファイル: `crates/relay/Cargo.toml`（serde_json）、`crates/relay/src/slack/events.rs`、`crates/relay/src/slack/events/tests.rs`、`crates/relay/src/slack/mod.rs`

## 公開 API

```rust
#[derive(Debug, PartialEq)]
pub enum Envelope {
    UrlVerification { challenge: String },
    Message { event_id: String, candidate: Option<Candidate> },
    Other,
}
#[derive(Debug, PartialEq, Clone)]
pub struct Candidate { pub chat_id: String, pub channel: String, pub thread_ts: String, pub user: String, pub text: String }
pub fn classify(body: &serde_json::Value) -> Envelope;

pub struct Dedupe { /* VecDeque + HashSet */ }
impl Dedupe {
    pub fn new(capacity: usize) -> Self;
    pub fn first_seen(&mut self, event_id: &str) -> bool; // true only the first time
}
```

## テスト（先に書く）

- `url_verification_detected`
- `thread_reply_is_candidate`: chat_id が `C123:1700000000.000100`
- `top_level_message_not_candidate`: `thread_ts` なし → `Message{candidate: None}`
- `thread_parent_not_candidate`: `thread_ts == ts`
- `bot_message_not_candidate`: `bot_id` あり
- `subtype_not_candidate`: `message_changed`、`channel_join`
- `non_message_event_is_other`
- `private_channel_message_same_as_public`
- `dedupe_drops_second`
- `dedupe_evicts_oldest`: capacity 2 で a, b, c の後 a は再び first_seen

## 完了条件

- 共通の完了条件
- コミット `feat(relay): classify slack events`
