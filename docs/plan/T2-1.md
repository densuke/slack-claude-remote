# T2-1 agent の MCP ディスパッチ

- モデル: sonnet
- 依存: T1-1
- 参照: spec §3、README §4.2
- 変更ファイル: `crates/agent/Cargo.toml`（protocol を path 依存、serde_json）、`crates/agent/src/mcp.rs`、`crates/agent/src/mcp/tests.rs`、`crates/agent/src/main.rs`（`mod mcp;` の追加のみ）

## 公開 API

```rust
pub enum Outcome {
    Respond(serde_json::Value),              // write one line to stdout
    Emit(protocol::AgentMsg),                // send to relay
    Both(serde_json::Value, protocol::AgentMsg),
    Ignore,
}
pub const INSTRUCTIONS: &str;
pub fn dispatch(line: &str) -> Outcome;
pub fn channel_notification(chat_id: &str, user: &str, text: &str) -> serde_json::Value;
pub fn permission_notification(request_id: &str, behavior: protocol::Behavior) -> serde_json::Value;
```

## テスト（先に書く）

- `initialize_declares_channel_capabilities`: `result.capabilities.experimental` に `claude/channel` と `claude/channel/permission`、`result.capabilities.tools` がある、`instructions` が空でない
- `initialize_echoes_protocol_version`: 要求の `params.protocolVersion` をそのまま返す
- `response_echoes_id`: 数値 id と文字列 id の両方
- `tools_list_has_only_reply`: 1 件、name `reply`、required が `chat_id` と `text`
- `reply_call_emits_agent_reply_and_responds`: `Both`、`content[0].text == "sent"`
- `reply_call_missing_text_is_invalid_params`: `error.code == -32602`
- `unknown_tool_is_invalid_params`
- `ping_returns_empty_object`
- `unknown_method_is_32601`
- `initialized_notification_ignored`
- `unknown_notification_ignored`
- `permission_request_notification_emits`: 4 フィールドが `AgentMsg::PermissionRequest` に入る
- `channel_notification_shape`: method、`params.content`、`params.meta` のキーが `chat_id` と `user` だけ。`id` キーがない
- `permission_notification_shape`
- `garbage_line_ignored`: 不正 JSON は `Ignore`

## 実装

- `serde_json::Value` ベースで書く。I/O をしない
- 通知とリクエストの区別は `id` キーの有無
- 300 行以内

## 完了条件

- 共通の完了条件
- コミット `feat(agent): add MCP channel dispatch`
